# The test fixture is built (with its own git history) by the test harness, not committed.
/tests/fixtures/*/
# breadcrumbs a wiki writes, should one ever be created inside the repo
.auto-commit-failed
.push-failed
.DS_Store

# Private org-term denylist for scripts/check-no-org.sh (ships nowhere)
/.org-denylist

__pycache__/
*.pyc

# IDE project files
.idea/
