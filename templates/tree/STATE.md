---
type: state
title: Current focus
timestamp: 2026-01-01
---

**The single hand-written surface is initiative frontmatter** (`initiatives/<slug>.md`:
`assignment_kind`, `assignment_status`, `priority`, `next`, `waiting_on`/`ask`). The board below is
generated from it by `hooks/board.py`, regenerated at every commit; never hand-edit between the
markers. A durable "what happened" note that isn't a frontmatter field goes to
`journal/YYYY-MM-DD.md` via `hooks/journal-append.sh`, not here. Enable the schema and its caps
with wiki.config.json's `initiative_schema` block once you have initiative pages to enforce it on
(see the plugin's docs); until then this file just carries the markers with nothing to show.

<!-- board:begin (generated by hooks/board.py from initiative frontmatter; do not edit) -->
(nothing yet — this is a fresh wiki, or initiative_schema isn't configured)
<!-- board:end -->

## Inbox (retired — do not add bullets here; a hard lint gate rejects them)
Write the initiative's frontmatter and a journal line instead.

## Map
- Conventions + entry map: [KNOWLEDGE.md](KNOWLEDGE.md)
- Long-horizon / parked: [ROADMAP.md](ROADMAP.md)
