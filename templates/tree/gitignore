# Generated + machine-local files — not part of the curated wiki.
/catalog.tsv
/.auto-commit-failed
/.push-failed
.DS_Store

# Local media archive referenced by transcript provenance NOTEs (meeting-notes skill).
# Recordings are too large and too raw for git history — keep only their text transcripts
# in sources/ and point source-origin: here.
/sources-media/

# Obsidian: keep the link-format config so it survives a fresh clone; ignore workspace caches.
.obsidian/workspace*.json
.obsidian/cache
.obsidian/plugins/*/data.json

# --- POWER-USER LAYOUT ONLY ---
# If your WIKI_ROOT is ~/.claude (shared with Claude Code's config dir), DELETE the lines above and
# use an ALLOWLIST instead, so machine/session/cache dirs never leak into the wiki repo:
#   /*
#   !/.gitignore
#   !/CLAUDE.md
#   !/KNOWLEDGE.md
#   !/STATE.md
#   !/ROADMAP.md
#   !/wiki.config.json
#   !/entities/  (and the other content dirs)
#   !/hooks/  !/skills/  !/commands/
# In that layout sources-media/ is already ignored by the /* rule — never add a
# !/sources-media/ re-include.
