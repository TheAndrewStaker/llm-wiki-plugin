Raw, immutable sources (transcripts, PDFs, clipped articles). Skills READ from here and never edit.
Date-bucket as `YYYY/MM/` when it grows. Synthesis pages elsewhere point back here via `synthesized_from:`.
